package main

import "fmt"

    func main() {

    var N int = 15

    fmt.Println("Program to find the sum of N natural numbers.")

    sum := N + 1

    var sum1 int = N * sum

    var sum2 int = sum1 / 2

    fmt.Print("The sum of the first " , N, " ", " natural number is ", sum2, " ")
}