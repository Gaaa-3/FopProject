package main

import "fmt"

func main() {
	num := 12321
	num1 := num
	res := 0
	res1 := 0
	res2 := 0
	for true {

		res1 = res * 10
		res2 = num1 % 10
		res = res1 + res2

		num1 = num1 / 10
		if (num1 == 0) {
			break
		}
	}

	fmt.Print(res)

	if (res == num) {
		fmt.Print(" Is Palindrome")
	}
	if ( res != num) {
		fmt.Print(" Not Palindrome")
	}
}
